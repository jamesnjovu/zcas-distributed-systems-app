/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
  basePath: '/zcas-distributed-systems-app',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
